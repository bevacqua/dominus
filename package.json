{
  "name": "dominus",
  "description": "Lean DOM Manipulation",
  "version": "6.0.0",
  "homepage": "https://github.com/bevacqua/dominus",
  "author": {
    "email": "ng@bevacqua.io",
    "name": "Nicolas Bevacqua",
    "url": "http://bevacqua.io"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/bevacqua/dominus.git"
  },
  "main": "src/null.js",
  "browser": "src/dominus.js",
  "scripts": {
    "build": "jshint . && browserify -s dominus -do dist/dominus.js src/dominus.js && uglifyjs -m -c -o dist/dominus.min.js dist/dominus.js",
    "deploy": "npm run build && npm run deployment",
    "deployment": "git add dist && npm version ${BUMP:-\"patch\"} --no-git-tag-version && git add package.json && git commit -am \"Autogenerated pre-deployment commit\" && bower version ${BUMP:-\"patch\"} && git reset HEAD~2 && git add . && git commit -am \"Release $(cat package.json | jq -r .version)\" && git push --tags && npm publish && git push"
  },
  "devDependencies": {
    "browserify": "11.0.1",
    "jshint": "2.8.0",
    "uglify-js": "2.4.24"
  },
  "dependencies": {
    "crossvent": "1.5.4",
    "poser": "1.2.0",
    "sektor": "1.1.4"
  }
}
